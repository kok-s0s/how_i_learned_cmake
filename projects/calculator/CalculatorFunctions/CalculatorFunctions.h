#include <cmath>
#include <cstring>
#include <iostream>
#include <queue>
#include <stack>

using std::cout;
using std::endl;
using std::queue;
using std::stack;
using std::string;

void trim(string &);
void sayHi();
int priority(char);
void handle(string);
void sayBye();